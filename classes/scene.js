class MainScene extends BABYLON.Scene{
    
    backButton;
    cameraAlpha;
    cameraBeta;
    cameraRadius;
    cameraPosition;
    selectedBall;
    
    constructor(engine){
        super(engine);


    }
}